const permissions_edit = 'edit-permissions';
const permissions_show = 'show-permissions';

export const PermissionsOperation = {
	READ: [permissions_edit, permissions_show],
	UPDATE: [permissions_edit],
};
