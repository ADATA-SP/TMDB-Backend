services:
    app:
        build:
            context: .
            dockerfile: .adata/Dockerfile
            target: development
        env_file:
            - .env
        ports:
            - '${APP_PORT:-3001}:${APP_PORT:-3001}'
        restart: unless-stopped
